import React from "react";
import Button from "./Button";

export default class Main extends React.Component {
    render() {
        return (
            <div>
                <Button />
            </div>
        )
        
    }
}